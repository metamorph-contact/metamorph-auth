{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "BrowserGenerationCancellationRequestV1",
  "description": "Direct, capability-authenticated exact-generation cancellation delivery.",
  "type": "object",
  "properties": {
    "capability": {
      "$ref": "#/$defs/BrowserGenerationCancellationCapability"
    },
    "schemaVersion": {
      "type": "integer",
      "format": "uint16",
      "maximum": 65535,
      "minimum": 0
    }
  },
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "capability"
  ],
  "$defs": {
    "BrowserGenerationCancellationCapability": {
      "type": "string",
      "maxLength": 12288,
      "minLength": 5,
      "pattern": "^[A-Za-z0-9_-]+(?:\\.[A-Za-z0-9_-]*){2}(?:(?:\\.[A-Za-z0-9_-]*){2})?$"
    }
  }
}
